apiVersion: apps/v1
kind: Deployment
metadata:
  name: ci-cd-javascript-app
  labels:
    app: ci-cd-javascript-app
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ci-cd-javascript-app
  template:
    metadata:
      labels:
        app: ci-cd-javascript-app
    spec:
      containers:
      - name: ci-cd-javascript-app
        image: ci-cd-javascript-app:latest
        ports:
        - containerPort: 3000
          protocol: TCP
        env:
        - name: PORT
          value: "3000"
        - name: NODE_ENV
          value: "production"
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 5
          periodSeconds: 5

