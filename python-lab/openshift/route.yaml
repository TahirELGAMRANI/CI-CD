apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: ci-cd-python-app
  labels:
    app: ci-cd-python-app
spec:
  to:
    kind: Service
    name: ci-cd-python-app
  port:
    targetPort: http
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect

